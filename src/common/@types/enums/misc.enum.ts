export enum CursorType {
  DATE = 'DATE',
  STRING = 'STRING',
  NUMBER = 'NUMBER',
}

export enum QueryOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum PaginationType {
  OFFSET = 'OFFSET',
  CURSOR = 'CURSOR',
}

export enum PostStateEnum {
  DRAFT = 'DRAFT',
  PUBLISHED = 'PUBLISHED',
}
